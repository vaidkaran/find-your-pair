<h1>My Projects</h1>

<p>
  <% current_user.projects.each do |p| %>
    <p>
      <%= p.name %>
      <%= link_to "Show", project_path(p.id) %>
      <%= link_to 'Delete', project_path(p.id), method: :delete, data: {confirm: "You sure?"} %>
    </p>
  <% end %>
</p>

